To start:
node app.js

To stop:
control + c

To view data
http://localhost:8000/cryptos

Note that this server runs on port 8000, not 3000 like the main server.
That way both can be ran at the same time.
